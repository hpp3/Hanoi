#ifndef constants_
#define constants_
const int num = 10; //number of disks must not exceed 600; game is unplayable above 50;
const int scry = 720, scrx = 960;
const int disk_height = int(0.9*scry/(num+2));

#endif
